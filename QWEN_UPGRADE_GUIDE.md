# Qwen-Plus AI 升级指南

## 🎯 概述

原系统使用简单的关键词匹配和情感分析来评估导师。新版本集成了阿里云 Qwen-Plus 大语言模型，能够更准确地分析评论并提供专业建议。

## 🆕 主要改进

### 对比：原系统 vs AI 增强版

| 特性 | 原系统 | Qwen-Plus 增强版 |
|------|--------|------------------|
| 评分方法 | 关键词匹配 | AI 深度理解 |
| 准确性 | 中等 | 高 |
| 维度理由 | 无 | 有详细说明 |
| 整体建议 | 无 | AI 生成建议 |
| 处理速度 | 快（秒级） | 慢（需API调用） |
| 成本 | 免费 | 需 API 费用 |

## 📁 文件说明

### 新增文件

- **[data_processor_qwen.py](data_processor_qwen.py:1)** - 使用 Qwen-Plus 的增强版数据处理器

### 原有文件

- **[data_processor.py](data_processor.py:1)** - 原版关键词匹配处理器（已修复 NaN 问题）
- **[generate_web_data.py](generate_web_data.py:1)** - Web 数据生成器（已修复 NaN 问题）

## 🚀 使用方法

### 方法 1: 使用 Qwen-Plus AI 模型（推荐）

#### 1. 安装依赖

```bash
pip3 install openai pandas xlrd openpyxl
```

#### 2. 获取 API Key

访问阿里云 DashScope 平台获取 API Key:
- 网址: https://dashscope.aliyun.com/
- 注册并创建 API Key（格式：sk-xxx）

#### 3. 运行 AI 增强版处理器

```bash
python3 data_processor_qwen.py
```

#### 4. 选择处理模式

程序会提示您选择：

```
1. 测试模式（处理前10位导师）     - 快速测试，约1分钟
2. 小批量模式（处理前100位导师）   - 中等规模，约10分钟
3. 完整模式（处理所有9392位导师）  - 完整处理，约2-3小时
```

**建议：**
- 首次使用：选择测试模式（模式 1）
- 正式使用：选择完整模式（模式 3），但需要注意 API 费用

#### 5. 生成的文件

```
mentor_metrics_ai_10.json     # 测试模式输出
mentor_metrics_ai_100.json    # 小批量模式输出
mentor_metrics_ai_9392.json   # 完整模式输出
```

### 方法 2: 使用原版关键词匹配（免费、快速）

```bash
# 1. 处理数据
python3 data_processor.py

# 2. 生成 Web 数据
python3 generate_web_data.py
```

生成的文件：
- `mentor_metrics.json` - 导师指标
- `docs/data/` - Web 数据目录

## 📊 AI 增强版输出格式

### 示例输出

```json
{
  "mentor-id-xxx": {
    "id": "xxx",
    "name": "张三",
    "school": "清华大学",
    "department": "计算机科学与技术系",
    "evaluationCount": 15,
    "dimensionScores": {
      "导师能力": 8.5,
      "经费情况": 7.0,
      "学生补助": 6.5,
      "师生关系": 4.0,
      "工作时间": 3.5,
      "毕业去向": 7.5
    },
    "dimensionReasons": {
      "导师能力": "科研能力强，发表多篇顶会论文",
      "经费情况": "经费较充足，项目多",
      "学生补助": "补助一般，约2000-3000/月",
      "师生关系": "要求严格，学生压力大",
      "工作时间": "经常需要加班，休息时间少",
      "毕业去向": "学生毕业去向较好，多数进入大厂"
    },
    "totalScore": 6.2,
    "overallRecommendation": "导师科研能力强，适合有志于学术研究的学生，但需做好压力大的心理准备",
    "evaluations": [...]
  }
}
```

### 新增字段说明

| 字段 | 说明 |
|------|------|
| `dimensionReasons` | 每个维度的评分理由（AI 生成） |
| `overallRecommendation` | 整体建议（AI 生成） |

## 🔄 将 AI 结果集成到网站

### 步骤 1: 运行 AI 处理器

```bash
python3 data_processor_qwen.py
# 选择模式 3（完整模式）
# 等待处理完成（约2-3小时）
```

### 步骤 2: 替换数据文件

```bash
# 备份原数据
mv mentor_metrics.json mentor_metrics_old.json

# 使用 AI 生成的数据
cp mentor_metrics_ai_9392.json mentor_metrics.json
```

### 步骤 3: 重新生成 Web 数据

```bash
python3 generate_web_data.py
```

### 步骤 4: 测试网站

```bash
cd docs
python3 -m http.server 8000
# 访问 http://localhost:8000
```

## 💰 成本估算

### API 调用成本

根据阿里云 Qwen-Plus 定价：
- 输入：约 0.004 元 / 1K tokens
- 输出：约 0.012 元 / 1K tokens

### 预估成本

每位导师处理：
- 平均输入：约 1500 tokens
- 平均输出：约 500 tokens
- 单次成本：约 0.012 元

**全量处理（9,392 位导师）：**
- 预估总成本：**约 110-150 元**

**建议：**
1. 先用测试模式验证效果
2. 确认满意后再运行完整模式
3. 定期增量更新，而非全量重跑

## 🎨 前端显示优化

### 修改建议（可选）

如果使用 AI 增强版，可以在前端显示更多信息：

#### 1. 显示评分理由

在 [mentor-detail.js](docs/js/mentor-detail.js:1) 中，修改 `renderDimensionLegend` 函数：

```javascript
function renderDimensionLegend(dimensionScores, dimensionReasons) {
    const legend = document.getElementById('dimensionLegend');

    legend.innerHTML = Object.entries(dimensionScores)
        .map(([name, score]) => {
            const reason = dimensionReasons?.[name] || '';
            return `
                <div class="legend-item" title="${escapeHtml(reason)}">
                    <span class="legend-label">${escapeHtml(name)}</span>
                    <span class="legend-score" style="color: ${getScoreColor(score)}">
                        ${formatScore(score)}
                    </span>
                </div>
            `;
        }).join('');
}
```

#### 2. 显示 AI 建议

在建议部分添加 AI 生成的推荐：

```javascript
if (mentorData.overallRecommendation) {
    suggestions.push({
        title: '🤖 AI 综合建议',
        text: mentorData.overallRecommendation
    });
}
```

## 🔧 故障排除

### 问题 1: API Key 无效

```
Error: Invalid API key
```

**解决方案：**
- 检查 API Key 格式（应为 sk-xxx）
- 确认 API Key 已激活
- 检查账户余额

### 问题 2: API 调用超时

```
Error: Request timeout
```

**解决方案：**
- 检查网络连接
- 增加超时时间
- 减小批处理大小

### 问题 3: JSON 解析错误

```
Error: JSON decode error
```

**解决方案：**
- AI 响应格式可能不标准
- 代码会自动回退到默认评分
- 查看日志确认具体导师

## 📈 性能优化

### 批处理建议

```python
# 修改 data_processor_qwen.py 中的延迟时间
processor.process_evaluations_with_ai(
    sample_size=None,
    delay=0.2  # 减小延迟（注意 API 限流）
)
```

### 多线程处理（高级）

```python
import concurrent.futures

# 使用线程池并行处理
with concurrent.futures.ThreadPoolExecutor(max_workers=5) as executor:
    futures = [
        executor.submit(process_mentor, mentor_id, data)
        for mentor_id, data in mentor_list
    ]
```

**注意：** 需要注意 API 的 QPS 限制

## 📝 最佳实践

### 1. 开发流程

```bash
# 1. 测试模式验证
python3 data_processor_qwen.py  # 选择模式 1

# 2. 检查结果
cat mentor_metrics_ai_10.json

# 3. 确认无误后运行完整模式
python3 data_processor_qwen.py  # 选择模式 3

# 4. 集成到网站
mv mentor_metrics.json mentor_metrics_old.json
cp mentor_metrics_ai_9392.json mentor_metrics.json
python3 generate_web_data.py
```

### 2. 定期更新

```bash
# 只处理新增/更新的导师（需要自己实现增量逻辑）
# 或定期（如每月）重跑一次完整模式
```

### 3. 数据备份

```bash
# 备份重要数据
cp mentor_metrics.json backup/mentor_metrics_$(date +%Y%m%d).json
cp -r docs/data backup/data_$(date +%Y%m%d)
```

## 🎓 技术细节

### Qwen-Plus 模型参数

```python
response = self.client.chat.completions.create(
    model="qwen-plus",
    messages=[...],
    temperature=0.3,     # 较低温度，保证输出稳定
    max_tokens=1000      # 限制输出长度
)
```

### Prompt 工程

Prompt 设计要点：
1. 明确角色定位（研究生导师评价专家）
2. 清晰的评分标准
3. 严格的输出格式要求
4. 示例和说明

### 错误处理

系统包含三层错误处理：
1. API 调用异常 → 返回默认评分
2. JSON 解析失败 → 尝试提取 JSON
3. 网络超时 → 重试机制

## 🔮 未来改进方向

1. **增量更新机制**
   - 只处理新增评论
   - 缓存已处理的导师

2. **多模型支持**
   - GPT-4
   - Claude
   - 本地模型（LLaMA）

3. **自动化流程**
   - GitHub Actions 定时任务
   - 自动检测数据更新

4. **A/B 测试**
   - 同时展示关键词版和 AI 版
   - 收集用户反馈

## 📞 支持

如有问题，请：
1. 查看本文档的故障排除部分
2. 检查代码注释
3. 提交 GitHub Issue

---

**更新时间**: 2026-02-10
**版本**: v2.0
